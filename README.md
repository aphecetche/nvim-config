Configuration Neovim _is_ a somewhat big task.

Did not want a ready-made distribution (e.g. LunarVim or LazyVim) as I wanted to understand (most of) my configuration. But then doing it from scratch required a lot of reading and some choices on how to organize the configuration directories and files. 

After some fooling around, I came to this repo : 

`https://github.com/chrisgrieser/.config/tree/main/nvim`

from which I took a lot of inspiration... (with some names I prefer, e.g. for `config` I use `core`)





